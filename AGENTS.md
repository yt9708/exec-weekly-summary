# AGENTS.md — 给后续开发 AI 的使用说明

> 本文件面向所有参与此项目的 AI 开发助手，描述了项目的文件夹结构和使用规范。

---

## 一、项目目录结构

```
exec-weekly-summary/
├── AGENTS.md           ← 本文件：AI 开发说明
├── README.md           ← 项目简要说明
├── CLAUDE.md           ← CodeBuddy 使用说明
├── docs/               ← 产品文档区
│   ├── prd/            ←   PRD 文档
│   ├── decisions/      ←   关键决策记录
│   └── *.md            ←   需求迭代、部署指南等
├── assets/             ← 素材区
│   ├── design/         ←   效果图、UI 参考、设计稿
│   ├── bug/            ←   测试报错截图
│   └── reference/      ←   参考图、灵感收集
├── notes/              ← 学习笔记区
│   └── *.md            ←   开发踩坑记录、技术方案
└── src/                ← 代码区（所有代码仅在此处）
    ├── app.js          ←   应用入口
    ├── routes/         ←   路由模块
    ├── services/       ←   业务服务
    ├── views/          ←   EJS 模板视图
    ├── public/         ←   静态资源
    ├── data/           ←   数据文件（JSON）
    ├── snapshots/      ←   推送截图
    ├── package.json    ←   依赖管理
    └── Dockerfile      ←   容器化配置
```

---

## 二、各文件夹用途与规则

### 1. `docs/` — 产品文档区

- **用途**：存放所有产品相关文档，包括 PRD、需求迭代记录、关键决策记录、部署指南等。
- **规则**：
  - PRD 放在 `docs/prd/` 下，命名格式：`PRD-<功能模块名>-v<版本号>.md`
  - 关键决策记录放在 `docs/decisions/`，格式：`ADR-<主题>.md`
  - 所有文档必须使用 Markdown 格式
  - 开始任何新功能开发前，先在 `docs/` 中创建或更新需求文档

### 2. `assets/` — 素材区

- **`assets/design/`**：UI 设计稿截图、Figma 导出图、设计原型 HTML 等
- **`assets/bug/`**：测试过程遇到的功能/UI 异常截图，命名格式：`<BUG编号>-<简述>.png`
- **`assets/reference/`**：参考图、竞品截图、灵感收集
- **规则**：图片优先使用 PNG 格式；涉及敏感信息需打码处理

### 3. `notes/` — 学习笔记

- **用途**：记录开发过程中踩过的坑、解决的技术难题、学到的架构方案
- **规则**：
  - 每个问题一个 Markdown 文件，命名简洁明了
  - 建议内容：背景、现象、根因分析、解决方案、总结
  - 内容可口语化，不拘泥格式，但需保证后续 AI 能读懂

### 4. `src/` — 代码区（核心规则）

- **所有代码文件必须放在 `src/` 下**，严禁在根目录或其他非代码目录写代码
- **代码不得引用 `src/` 外部的文件**（包括图片、JSON 等资源）
- 当前技术栈：**Node.js + Express + EJS**（不是前后端分离项目）
- 所有依赖在 `src/package.json` 中统一管理
- 新增路由放在 `src/routes/`，新增服务放在 `src/services/`

---

## 三、开发通用规则

### 3.1 文档优先
- 新功能开发 → 先在 `docs/` 中创建需求文档
- 技术选型/架构决策 → 在 `docs/decisions/` 中记录 ADR
- Bug 修复 → 截图放在 `assets/bug/`，分析过程可记入 `notes/`

### 3.2 代码质量
- 遵循项目已有的代码风格
- 关键逻辑写"为什么"注释，不写"是什么"注释
- 函数/变量命名需自文档化

### 3.3 AI 行为准则
- 每次开始工作前，先读取 `AGENTS.md` 和 `CLAUDE.md`
- 代码修改后检查是否引入错误
- 遇到不明确的代码或配置，可以提出现存问题及优化建议
- 在 `notes/` 中记录遇到的特殊问题和解决方案

---

## 四、快速启动

```bash
cd src
npm install
npm start
```

访问 http://127.0.0.1:3456

查看 `docs/` 下 PRD 了解产品需求，查阅 `notes/` 看是否有踩坑记录。

---

*最后更新：2026-06-05*
