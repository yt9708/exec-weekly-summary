# AGENTS.md — AI 开发助手使用说明

> 本文档面向后续参与此项目的所有 AI 开发助手，描述了项目的工作区结构和使用规范。

---

## 一、项目目录结构总览

```
exec-weekly-summary/
├── AGENTS.md           # → 本文件：AI 开发说明
├── README.md           # → 项目简要介绍
├── docs/               # 文档区
│   ├── prd/            #   PRD 文档
│   ├── requirements/   #   需求迭代记录
│   └── decisions/      #   关键决策记录
├── assets/             # 素材区
│   ├── design/         #   效果图、UI 参考、设计稿
│   ├── bug/            #   测试报错截图
│   └── reference/      #   参考图、灵感收集
├── notes/              # 学习笔记区
│   └── *.md            #   开发中踩过的坑、技术方案记录
└── src/                # 代码区（所有代码仅放在此处）
    ├── client/         #   前端代码
    ├── server/         #   后端代码
    ├── shared/         #   前后端共享代码（类型定义、工具函数等）
    └── config/         #   项目配置文件
```

---

## 二、文件夹使用规范

### 1. `docs/` — 产品文档区

- **用途**：存放所有产品相关文档，包括 PRD、需求迭代记录、关键决策记录。
- **规则**：
  - 每个需求或功能模块应有独立的 Markdown 文档。
  - PRD 放在 `docs/prd/` 下，命名格式：`PRD-<功能模块名>-v<版本号>.md`。
  - 需求迭代记录放在 `docs/requirements/`。
  - 关键决策记录放在 `docs/decisions/`，格式为 ADR（Architecture Decision Record）。
  - **所有文档必须使用 Markdown 格式**，避免使用 Word/PDF 等其他格式直接提交。

### 2. `assets/` — 素材区

- **`assets/design/`**：存放 UI 设计稿截图、Figma 导出图、设计规范参考图等。
- **`assets/bug/`**：存放测试过程中遇到的 UI/功能异常截图，命名格式：`<BUG编号>-<简述>.png`。
- **`assets/reference/`**：存放参考图、竞品截图、灵感收集。
- **规则**：图片文件优先使用 PNG 格式；涉及敏感信息的截图需打码处理。

### 3. `notes/` — 学习笔记

- **用途**：记录开发过程中踩过的坑、解决的技术难题、学到的架构方案等。
- **规则**：
  - 每个问题单独一个 Markdown 文件，命名简洁明了。
  - 建议包含：背景、现象、根因分析、解决方案、总结。
  - 内容可以口语化，不拘泥格式，但需保证后续开发 AI 能读懂。

### 4. `src/` — 代码区

- **规则（严格遵守）**：
  - **所有代码文件必须放在 `src/` 下**，严禁在根目录或其他非代码目录下写代码。
  - `src/client/`：存放前端代码。
  - `src/server/`：存放后端代码。
  - `src/shared/`：存放前后端共享的类型定义、常量、工具函数等。
  - `src/config/`：存放项目配置文件（如数据库连接、环境变量示例等）。
  - 所有代码不得引用 `src/` 外部的文件。
  - 依赖管理：前端 package.json 在 `src/client/` 下，后端在 `src/server/` 下（或使用 monorepo 统一管理）。

---

## 三、开发通用规则

### 3.1 文档优先
- 开始任何新功能开发前，先在 `docs/` 中创建或更新需求文档。
- 做技术选型或架构决策时，在 `docs/decisions/` 中记录 ADR。

### 3.2 代码质量
- 遵循项目中约定的代码风格（ESLint + Prettier）。
- 提交前确保无 TypeScript 编译错误。
- 关键逻辑需写注释，但避免"什么"注释，多写"为什么"注释。
- 组件/函数命名需自文档化，不建议额外注释。

### 3.3 测试
- 单元测试放在 `src/<模块>/__tests__/` 下。
- 端到端测试或 API 测试放在 `src/test/` 下。

### 3.4 版本管理
- 遵循语义化版本（SemVer）。
- 每次有意义的变更需更新 `CHANGELOG.md`（如不存在则新建在根目录）。
- 不要手动修改 `package-lock.json` 或 `yarn.lock`。

### 3.5 AI 开发助手行为准则
- 每次读取代码后，先确认当前工作目录和文件结构。
- 如遇到不明确的需求，优先查阅 `docs/` 下的相关文档。
- 代码修改后自动检查编译/类型错误。
- 若发现不合理的遗留代码或配置，可以提出现存问题及优化建议。
- 在 `notes/` 中记录开发过程中遇到的特殊问题和解决方案，方便后续 AI 快速学习。

---

## 四、快速启动指引

1. 查看 `README.md` 了解项目概述。
2. 查阅 `docs/` 下的 PRD 了解产品需求。
3. 进入 `src/` 查看代码入口文件。
4. 遇到问题先看 `notes/` 是否有踩坑记录。

---

*最后更新：2026-05-15*
